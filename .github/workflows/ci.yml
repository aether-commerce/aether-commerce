name: Aether CI

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: aether-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Aether CI / validate
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
      - name: Fetch develop for changeset diverge-check
        # --update-head-ok: this workflow also runs on `push: branches: [develop]`,
        # where HEAD already *is* local branch develop - a plain fetch into that
        # same ref is refused ("refusing to fetch into current branch") without it.
        run: git fetch --update-head-ok origin develop:develop
      - uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa # v4
        with:
          version: 8.15.9
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Enforce release policy
        env:
          GITHUB_BASE_REF: ${{ github.base_ref }}
          GITHUB_HEAD_REF: ${{ github.head_ref }}
        run: node scripts/check-release-policy.mjs
      - name: Secret scan
        run: |
          if git grep -n -I -E '(sk|rk)_(live|test)_[A-Za-z0-9]{16,}|whsec_[A-Za-z0-9]{16,}|AIza[0-9A-Za-z_-]{30,}|AKIA[0-9A-Z]{16}|-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----'; then
            echo "::error::Potential live credential found in a tracked file."
            exit 1
          fi
      - name: Typecheck, lint, unit and contract tests, and OpenAPI
        run: pnpm validate
      - name: Verify release metadata
        env:
          GITHUB_BASE_REF: ${{ github.base_ref }}
        run: |
          pnpm check:changesets
          if [ -n "$GITHUB_BASE_REF" ] && git diff --name-status "origin/${GITHUB_BASE_REF}...HEAD" | grep -Eq '^D[[:space:]]+\.changeset/[^/]+\.md$'; then
            echo "Version release detected; the consumed changesets were validated before versioning."
          elif git diff --name-status HEAD^1 HEAD | grep -Eq '^D[[:space:]]+\.changeset/[^/]+\.md$'; then
            echo "Version release commit detected; the consumed changesets were validated before versioning."
          elif find .changeset -maxdepth 1 -type f -name '*.md' ! -name README.md -print -quit | grep -q .; then
            pnpm changeset status
          else
            echo "No pending changesets; release metadata is already validated or no distributable package changed."
          fi
      - name: Validate LangGraph assistant
        run: |
          pnpm --filter @aether-commerce/ai-assistant typecheck
          pnpm --filter @aether-commerce/ai-assistant lint
          pnpm --filter @aether-commerce/ai-assistant test
          pnpm --filter @aether-commerce/ai-assistant build
      - name: Build workspace
        env:
          NEXT_PUBLIC_AETHER_API_URL: https://api.example.com
          NEXT_PUBLIC_AETHER_AI_URL: https://ai.example.com
          NEXT_PUBLIC_PORTFOLIO_URL: https://portfolio.example.com
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_Y2xlcmsuZXhhbXBsZS5jb20k
        run: pnpm build
      - name: Build storefront Worker bundle
        env:
          NEXT_PUBLIC_AETHER_API_URL: https://api.example.com
          NEXT_PUBLIC_AETHER_AI_URL: https://ai.example.com
          NEXT_PUBLIC_PORTFOLIO_URL: https://portfolio.example.com
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_Y2xlcmsuZXhhbXBsZS5jb20k
        run: pnpm --filter @aether-commerce/storefront build:cloudflare
      - name: Verify outputs
        run: |
          test -f apps/api/dist/index.js
          test -f apps/storefront/.open-next/worker.js
          test -f apps/admin/out/index.html
      - name: Install Playwright browser
        run: pnpm exec playwright install chromium
      - name: Assistant widget E2E
        run: pnpm test:e2e:assistant
